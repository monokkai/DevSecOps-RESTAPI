apiVersion: v1
kind: Service
metadata:
  name: {{ include "devsecops.fullname" . }}
  labels:
    {{- include "devsecops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devsecops.selectorLabels" . | nindent 4 }}
